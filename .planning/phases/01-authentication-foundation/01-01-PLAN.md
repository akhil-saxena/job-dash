---
phase: 01-authentication-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - wrangler.jsonc
  - tsconfig.json
  - tsconfig.app.json
  - tsconfig.worker.json
  - biome.json
  - .gitignore
  - .dev.vars.example
  - worker/index.ts
  - src/client/main.tsx
  - src/client/App.tsx
  - src/client/index.css
autonomous: true
requirements:
  - AUTH-03
must_haves:
  truths:
    - "Running `npm run dev` starts a local Vite dev server that serves the React SPA and proxies /api/* to the Hono Worker"
    - "GET /api/health returns { ok: true } JSON response"
    - "Navigating to localhost:5173 shows a React placeholder page"
  artifacts:
    - path: "package.json"
      provides: "All Phase 1 dependencies installed"
      contains: "better-auth"
    - path: "wrangler.jsonc"
      provides: "D1 binding, nodejs_compat flag, SPA fallback config"
      contains: "nodejs_compat"
    - path: "vite.config.ts"
      provides: "Vite 8 + React + Tailwind + Cloudflare plugin config"
      contains: "cloudflare"
    - path: "worker/index.ts"
      provides: "Hono app entry point with /api/health route"
      exports: ["default"]
    - path: "src/client/App.tsx"
      provides: "React SPA root component"
  key_links:
    - from: "vite.config.ts"
      to: "worker/index.ts"
      via: "cloudflare() plugin auto-detects wrangler.jsonc"
      pattern: "cloudflare\\(\\)"
    - from: "wrangler.jsonc"
      to: "worker/index.ts"
      via: "main field points to worker entry"
      pattern: "worker/index\\.ts"
---

<objective>
Scaffold the JobDash project from scratch: Vite 8 + React 19 + Hono + Cloudflare Worker + D1 binding + Tailwind CSS 4 + Biome + TypeScript. After this plan, `npm run dev` boots a working local dev environment with a React SPA served by a Cloudflare Worker and a `/api/health` endpoint responding to requests.

Purpose: Establish the runtime foundation (per D-08, D-09) that all subsequent plans build on. Without this, nothing else can be built.
Output: A working Vite + Cloudflare Worker project with all Phase 1 dependencies installed and configured.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-authentication-foundation/01-CONTEXT.md
@.planning/phases/01-authentication-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vite 8 project with all Phase 1 dependencies</name>
  <files>package.json, .gitignore, tsconfig.json, tsconfig.app.json, tsconfig.worker.json</files>
  <read_first>
    .planning/phases/01-authentication-foundation/01-RESEARCH.md (Standard Stack section for exact versions)
    .planning/phases/01-authentication-foundation/01-CONTEXT.md (D-08 through D-13 for project structure decisions)
  </read_first>
  <action>
    1. Scaffold Vite React-TS project in the repo root (NOT in a subdirectory — this IS the project root). The repo already has `.planning/`, `CLAUDE.md`, etc. Run from the repo root:
       ```
       npm create vite@latest . -- --template react-ts
       ```
       If it asks about non-empty directory, confirm overwrite. This creates package.json, tsconfig files, vite.config.ts stub, src/ directory.

    2. Install production dependencies (per D-08 single project, per research Standard Stack):
       ```
       npm install hono @hono/zod-validator
       npm install drizzle-orm better-auth
       npm install zod nanoid resend
       ```

    3. Install dev dependencies:
       ```
       npm install -D wrangler @cloudflare/vite-plugin @cloudflare/workers-types
       npm install -D drizzle-kit
       npm install -D @biomejs/biome
       npm install -D tailwindcss @tailwindcss/vite @vitejs/plugin-react
       ```

    4. Update `.gitignore` to include:
       ```
       node_modules/
       dist/
       .wrangler/
       .dev.vars
       .mf/
       *.local
       .DS_Store
       ```

    5. Create `tsconfig.worker.json`:
       ```json
       {
         "compilerOptions": {
           "target": "ES2022",
           "module": "ES2022",
           "moduleResolution": "bundler",
           "lib": ["ES2022"],
           "types": ["@cloudflare/workers-types/2023-07-01"],
           "strict": true,
           "skipLibCheck": true,
           "outDir": "./dist",
           "rootDir": ".",
           "resolveJsonModule": true,
           "isolatedModules": true,
           "esModuleInterop": true,
           "forceConsistentCasingInFileNames": true,
           "paths": {
             "@/*": ["./src/*"]
           }
         },
         "include": ["worker/**/*.ts", "src/server/**/*.ts", "src/db/**/*.ts", "src/shared/**/*.ts"]
       }
       ```

    6. Update `tsconfig.app.json` (Vite creates this) to add path alias:
       ```json
       {
         "compilerOptions": {
           "paths": { "@/*": ["./src/*"] }
         }
       }
       ```
       Keep existing compilerOptions from Vite scaffold, just add paths.

    7. Ensure `tsconfig.json` references both `tsconfig.app.json` and `tsconfig.worker.json`.

    8. Add scripts to package.json:
       ```json
       {
         "scripts": {
           "dev": "vite",
           "build": "vite build",
           "preview": "vite preview",
           "db:generate": "drizzle-kit generate",
           "db:migrate": "wrangler d1 migrations apply job-dash-db --local",
           "db:migrate:remote": "wrangler d1 migrations apply job-dash-db --remote",
           "deploy": "wrangler deploy",
           "lint": "biome check .",
           "lint:fix": "biome check --fix .",
           "format": "biome format --write ."
         }
       }
       ```
  </action>
  <verify>
    <automated>cd /Users/akhilsaxena/Documents/Repositories/akhil-saxena/job-track && node -e "const p = require('./package.json'); const deps = {...p.dependencies, ...p.devDependencies}; const required = ['hono','better-auth','drizzle-orm','zod','nanoid','resend','wrangler','@cloudflare/vite-plugin','@biomejs/biome','tailwindcss']; const missing = required.filter(r => !deps[r]); if(missing.length) { console.error('Missing:', missing); process.exit(1); } console.log('All deps present');"</automated>
  </verify>
  <acceptance_criteria>
    - package.json contains "hono", "better-auth", "drizzle-orm", "zod", "nanoid", "resend" in dependencies
    - package.json contains "wrangler", "@cloudflare/vite-plugin", "@biomejs/biome", "tailwindcss", "drizzle-kit" in devDependencies
    - package.json scripts include "dev", "build", "db:generate", "db:migrate", "deploy", "lint"
    - tsconfig.worker.json exists and contains "@cloudflare/workers-types"
    - .gitignore contains ".wrangler" and ".dev.vars" and "node_modules"
    - node_modules/ directory exists (deps installed successfully)
  </acceptance_criteria>
  <done>All Phase 1 dependencies installed, TypeScript configs created for both app and worker, package.json scripts defined</done>
</task>

<task type="auto">
  <name>Task 2: Configure Vite, Wrangler, Biome, Tailwind, and create Worker + SPA entry points</name>
  <files>vite.config.ts, wrangler.jsonc, biome.json, .dev.vars.example, worker/index.ts, src/client/main.tsx, src/client/App.tsx, src/client/index.css, src/vite-env.d.ts</files>
  <read_first>
    package.json (verify deps are installed from Task 1)
    .planning/phases/01-authentication-foundation/01-RESEARCH.md (Pattern 3: Wrangler config, Vite config example, project structure)
    .planning/phases/01-authentication-foundation/01-CONTEXT.md (D-08: single project, D-09: wrangler, D-13: timestamps)
  </read_first>
  <action>
    1. Create `vite.config.ts` (per research Vite Configuration example):
       ```typescript
       import { defineConfig } from "vite";
       import react from "@vitejs/plugin-react";
       import tailwindcss from "@tailwindcss/vite";
       import { cloudflare } from "@cloudflare/vite-plugin";
       import { resolve } from "node:path";

       export default defineConfig({
         plugins: [react(), tailwindcss(), cloudflare()],
         resolve: {
           alias: {
             "@": resolve(__dirname, "./src"),
           },
         },
       });
       ```

    2. Create `wrangler.jsonc` (per research Pattern 3 and D-09):
       ```jsonc
       {
         "$schema": "./node_modules/wrangler/config-schema.json",
         "name": "job-dash",
         "compatibility_date": "2026-04-15",
         "compatibility_flags": ["nodejs_compat"],
         "main": "./worker/index.ts",
         "assets": {
           "not_found_handling": "single-page-application",
           "run_worker_first": ["/api/*"]
         },
         "d1_databases": [
           {
             "binding": "DB",
             "database_name": "job-dash-db",
             "database_id": "local",
             "migrations_dir": "src/db/migrations"
           }
         ]
       }
       ```
       Note: `database_id` set to "local" for dev — user will replace with real ID after `wrangler d1 create`.

    3. Create `biome.json`:
       ```json
       {
         "$schema": "https://biomejs.dev/schemas/1.9.4/schema.json",
         "organizeImports": { "enabled": true },
         "linter": {
           "enabled": true,
           "rules": { "recommended": true }
         },
         "formatter": {
           "enabled": true,
           "indentStyle": "tab",
           "lineWidth": 100
         },
         "files": {
           "ignore": ["node_modules", "dist", ".wrangler"]
         }
       }
       ```

    4. Create `.dev.vars.example` (template for secrets — actual `.dev.vars` is gitignored):
       ```
       BETTER_AUTH_URL=http://localhost:5173
       BETTER_AUTH_SECRET=generate-a-random-string-at-least-32-chars
       GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
       GOOGLE_CLIENT_SECRET=your-google-client-secret
       RESEND_API_KEY=re_your_resend_api_key
       ```

    5. Create `worker/index.ts` — minimal Hono app with health check:
       ```typescript
       import { Hono } from "hono";

       type Env = {
         Bindings: {
           DB: D1Database;
           BETTER_AUTH_URL: string;
           BETTER_AUTH_SECRET: string;
           GOOGLE_CLIENT_ID: string;
           GOOGLE_CLIENT_SECRET: string;
           RESEND_API_KEY: string;
         };
       };

       const app = new Hono<Env>();

       app.get("/api/health", (c) => {
         return c.json({ ok: true, timestamp: Date.now() });
       });

       export default app;
       ```

    6. Move Vite's default `src/main.tsx` to `src/client/main.tsx` and update it:
       ```typescript
       import { StrictMode } from "react";
       import { createRoot } from "react-dom/client";
       import { App } from "./App";
       import "./index.css";

       createRoot(document.getElementById("root")!).render(
         <StrictMode>
           <App />
         </StrictMode>
       );
       ```

    7. Create `src/client/App.tsx`:
       ```typescript
       export function App() {
         return (
           <div className="flex min-h-screen items-center justify-center bg-stone-50">
             <div className="text-center">
               <h1 className="text-3xl font-bold text-stone-800">JobDash</h1>
               <p className="mt-2 text-stone-500">Loading...</p>
             </div>
           </div>
         );
       }
       ```

    8. Create `src/client/index.css`:
       ```css
       @import "tailwindcss";
       ```

    9. Update `index.html` in project root to point to `src/client/main.tsx` instead of `src/main.tsx`:
       ```html
       <script type="module" src="/src/client/main.tsx"></script>
       ```

    10. Create empty directories for the project structure:
        - `src/server/routes/`
        - `src/server/middleware/`
        - `src/server/lib/`
        - `src/db/schema/`
        - `src/db/migrations/`
        - `src/shared/`
        - `src/client/components/`
        - `src/client/lib/`
        - `src/client/pages/`

    11. If Vite scaffold created `src/App.tsx`, `src/App.css`, `src/main.tsx`, `src/index.css` — remove them (we moved to `src/client/`).
  </action>
  <verify>
    <automated>cd /Users/akhilsaxena/Documents/Repositories/akhil-saxena/job-track && npx vite build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - vite.config.ts contains `import { cloudflare } from "@cloudflare/vite-plugin"` and `cloudflare()` in plugins array
    - wrangler.jsonc contains `"nodejs_compat"` in compatibility_flags
    - wrangler.jsonc contains `"not_found_handling": "single-page-application"`
    - wrangler.jsonc contains `"run_worker_first": ["/api/*"]`
    - wrangler.jsonc contains `"binding": "DB"` in d1_databases
    - worker/index.ts contains `app.get("/api/health"` and `export default app`
    - src/client/main.tsx contains `createRoot` and imports `./App`
    - src/client/App.tsx contains `JobDash`
    - src/client/index.css contains `@import "tailwindcss"`
    - index.html contains `src/client/main.tsx`
    - .dev.vars.example contains `BETTER_AUTH_SECRET` and `GOOGLE_CLIENT_ID`
    - biome.json exists
    - `npx vite build` completes without errors
  </acceptance_criteria>
  <done>Vite dev server can build successfully, wrangler config ready for D1 + SPA, Worker entry point serves /api/health, React SPA renders placeholder, project directory structure established</done>
</task>

</tasks>

<verification>
1. `npx vite build` completes without error
2. worker/index.ts exports a Hono app with GET /api/health
3. All config files (vite.config.ts, wrangler.jsonc, biome.json, tsconfig.worker.json) exist and are syntactically valid
4. src/client/ directory contains main.tsx, App.tsx, index.css
5. .dev.vars.example documents all required env vars
</verification>

<success_criteria>
- `npx vite build` succeeds (exit code 0)
- `grep -l "cloudflare" vite.config.ts` returns the file
- `grep -l "nodejs_compat" wrangler.jsonc` returns the file
- `grep -l "/api/health" worker/index.ts` returns the file
- All 10 directories in project structure exist
</success_criteria>

<output>
After completion, create `.planning/phases/01-authentication-foundation/01-01-SUMMARY.md`
</output>
