---
phase: 03-frontend-shell-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vite.config.ts
  - index.html
  - src/client/index.css
  - src/client/hooks/useTheme.ts
  - src/client/lib/colors.ts
  - src/client/components/design-system/Badge.tsx
  - src/client/components/design-system/Button.tsx
  - src/client/components/design-system/Input.tsx
  - src/client/components/design-system/Card.tsx
  - src/client/components/design-system/ColumnHeader.tsx
  - src/client/components/design-system/SearchBar.tsx
  - src/client/components/design-system/FilterChips.tsx
  - src/client/components/design-system/TabBar.tsx
  - src/client/components/design-system/Modal.tsx
autonomous: true
requirements:
  - UI-01
  - UI-03
  - UI-04
  - UI-06

must_haves:
  truths:
    - "Design tokens generate working Tailwind utilities (bg-status-applied, text-text-primary, etc.)"
    - "Glass effect renders frosted surface on warm gradient background"
    - "Dark mode toggles without FOUC on page load"
    - "Dark mode persists across browser refresh via localStorage"
    - "System preference auto-detected on first visit"
    - "All 9 design system components render with correct variant props"
    - "Status colors are consistent across Badge and ColumnHeader"
  artifacts:
    - path: "src/client/index.css"
      provides: "Design tokens via @theme, glass utilities, dark mode @custom-variant, warm gradient body"
      contains: "@theme"
    - path: "src/client/hooks/useTheme.ts"
      provides: "Zustand theme store with localStorage persistence and system preference detection"
      exports: ["useTheme"]
    - path: "src/client/lib/colors.ts"
      provides: "Status color mapping constants used by Badge, ColumnHeader, kanban cards"
      exports: ["STATUS_COLORS", "STATUS_LABELS"]
    - path: "src/client/components/design-system/Badge.tsx"
      provides: "Badge component with filled/outlined/dot variants"
      exports: ["Badge"]
    - path: "src/client/components/design-system/Button.tsx"
      provides: "Button component with filled/outline/ghost variants"
      exports: ["Button"]
    - path: "src/client/components/design-system/Input.tsx"
      provides: "Input component with glass/raised variants"
      exports: ["Input"]
    - path: "src/client/components/design-system/Card.tsx"
      provides: "Glass card wrapper component"
      exports: ["Card"]
    - path: "src/client/components/design-system/Modal.tsx"
      provides: "Modal component (glass centered desktop, bottom sheet mobile)"
      exports: ["Modal"]
  key_links:
    - from: "index.html"
      to: "document.documentElement.classList"
      via: "inline script before CSS"
      pattern: "localStorage.theme"
    - from: "src/client/hooks/useTheme.ts"
      to: "document.documentElement.classList"
      via: "applyTheme function toggling dark class"
      pattern: "classList.toggle.*dark"
    - from: "src/client/index.css"
      to: "Tailwind utility classes"
      via: "@theme directive generating bg-status-*, text-text-*, etc."
      pattern: "@theme"
---

<objective>
Install Phase 3 dependencies, establish the complete design token system via Tailwind v4 @theme, implement dark mode with FOUC prevention, and build all 9 reusable design system components.

Purpose: This plan creates the visual foundation that every subsequent plan and phase imports. Design tokens, glass effects, dark mode, and all atomic/composed components must exist before layout or page work begins.

Output: Working design tokens generating Tailwind utilities, dark mode with system detection + manual toggle + localStorage persistence, and 9 design system components (Badge, Button, Input, Card, ColumnHeader, SearchBar, FilterChips, TabBar, Modal) ready for consumption.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-shell-dashboard/03-CONTEXT.md
@.planning/phases/03-frontend-shell-dashboard/03-RESEARCH.md
@.planning/phases/03-frontend-shell-dashboard/03-UI-SPEC.md
@.planning/phases/03-frontend-shell-dashboard/design-system.md

<interfaces>
<!-- Existing shared constants the design system depends on -->

From src/shared/constants.ts:
```typescript
export const APPLICATION_STATUSES = [
  "wishlist", "applied", "screening", "interviewing",
  "offer", "accepted", "rejected", "withdrawn",
] as const;
export type ApplicationStatus = (typeof APPLICATION_STATUSES)[number];

export const PRIORITIES = ["high", "medium", "low"] as const;
export type Priority = (typeof PRIORITIES)[number];
```

From src/client/lib/auth-client.ts:
```typescript
import { createAuthClient } from "better-auth/react";
export const authClient = createAuthClient({
  baseURL: window.location.origin,
  basePath: "/api/auth",
});
```

Current vite.config.ts (to be modified):
```typescript
import { defineConfig } from "vite";
import react from "@vitejs/plugin-react";
import tailwindcss from "@tailwindcss/vite";
import { cloudflare } from "@cloudflare/vite-plugin";
import { resolve } from "node:path";

export default defineConfig({
  plugins: [react(), tailwindcss(), cloudflare()],
  resolve: {
    alias: { "@": resolve(__dirname, "./src") },
  },
});
```

Current index.html (to be modified):
```html
<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>JobDash</title>
  </head>
  <body>
    <div id="root"></div>
    <script type="module" src="/src/client/main.tsx"></script>
  </body>
</html>
```

Current index.css (to be replaced):
```css
@import "tailwindcss";
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, design tokens, dark mode, and status colors</name>
  <files>
    package.json,
    vite.config.ts,
    index.html,
    src/client/index.css,
    src/client/hooks/useTheme.ts,
    src/client/lib/colors.ts
  </files>
  <read_first>
    package.json,
    vite.config.ts,
    index.html,
    src/client/index.css,
    src/shared/constants.ts,
    .planning/phases/03-frontend-shell-dashboard/03-RESEARCH.md (Pattern 2: Vite Config, Pattern 4: Design Tokens, Pattern 5: Zustand Theme Store, Pitfall 1: FOUC, Pitfall 3: Plugin Order)
  </read_first>
  <action>
**Step 1 — Install dependencies:**
```bash
npm install @tanstack/react-router @tanstack/react-query lucide-react zustand
npm install -D @tanstack/router-plugin @tanstack/react-router-devtools
```

**Step 2 — Update vite.config.ts:**
Add `tanstackRouter` plugin FIRST in the plugins array (per Pitfall 3). Configure it with `target: 'react'`, `autoCodeSplitting: true`, `generatedRouteTree: './src/client/routeTree.gen.ts'`, `routesDirectory: './src/client/routes'` (per Pitfall 9 — keep generated file inside tsconfig.app.json include scope). Keep react(), tailwindcss(), cloudflare() after it in that order.

**Step 3 — Update index.html for FOUC prevention (per D-21, Pitfall 1):**
Add an inline `<script>` in `<head>` BEFORE any other scripts:
```html
<script>
  document.documentElement.classList.toggle(
    "dark",
    localStorage.theme === "dark" ||
      (!("theme" in localStorage) &&
        window.matchMedia("(prefers-color-scheme: dark)").matches)
  );
</script>
```

**Step 4 — Create src/client/index.css with complete design token system (per D-10, D-11, D-20, D-22):**
- `@import "tailwindcss";`
- `@custom-variant dark (&:where(.dark, .dark *));` per D-20
- `@theme { }` block with ALL tokens from UI-SPEC:
  - `--font-sans`: Apple system font stack `-apple-system, BlinkMacSystemFont, 'SF Pro Display', 'SF Pro Text', 'Helvetica Neue', system-ui, sans-serif`
  - `--font-mono`: `'SF Mono', 'Menlo', 'Monaco', monospace`
  - Status colors: `--color-status-wishlist: #6b7280`, `--color-status-applied: #3b82f6`, `--color-status-screening: #8b5cf6`, `--color-status-interviewing: #f59e0b`, `--color-status-offer: #22c55e`, `--color-status-accepted: #10b981`, `--color-status-rejected: #ef4444`, `--color-status-withdrawn: #64748b`
  - Surface: `--color-surface-accent: #292524`
  - Text: `--color-text-primary: #1c1917`, `--color-text-secondary: #78716c`, `--color-text-muted: #a8a29e`
  - Star: `--color-star-filled: #f59e0b`, `--color-star-empty: #d6d3d1`
  - Dark surfaces: `--color-dark-dominant: #18181b`, `--color-dark-card: #27272a`, `--color-dark-accent: #f4f4f5`
  - Radius: `--radius-btn: 6px`, `--radius-card: 8px`, `--radius-input: 10px`, `--radius-card-mobile: 12px`, `--radius-modal: 14px`, `--radius-pill: 99px`
- `@layer components { }` with `.glass` and `.glass-hover` classes (both light and dark variants) per Research Pattern 4. Include `-webkit-backdrop-filter` for Safari (Pitfall 2).
- Body warm gradient background per D-11: `linear-gradient(145deg, #f5f3f0, #ece8e3, #e8e4df)`, `min-height: 100vh`
- Dark body: `#18181b`
- Global Lucide icon stroke: `.lucide { stroke-width: 1.8px; }` per D-23

**Step 5 — Create src/client/hooks/useTheme.ts (per D-21, Research Pattern 5):**
Zustand store with `mode: 'light' | 'dark' | 'system'` and `resolved: 'light' | 'dark'`. Functions: `getSystemTheme()` reads `prefers-color-scheme`, `resolveTheme()` maps mode to actual theme, `applyTheme()` toggles `.dark` class on `<html>`. Initialize from `localStorage.getItem('theme')` or 'system'. `setMode()` persists to localStorage and applies. Listen for `matchMedia` change event to update when in system mode. Export the `useTheme` hook and `ThemeMode` type.

**Step 6 — Create src/client/lib/colors.ts (per D-16, UI-04):**
Export `STATUS_COLORS` map: `Record<ApplicationStatus, string>` mapping each status to its hex color from UI-SPEC. Export `STATUS_LABELS` map: `Record<ApplicationStatus, string>` with display labels (capitalized). Export `STATUS_BADGE_BG` map: `Record<ApplicationStatus, string>` with rgba(.12) backgrounds for badges. Import `ApplicationStatus` from `@/shared/constants`.
  </action>
  <verify>
    <automated>cd /Users/akhilsaxena/Documents/Repositories/akhil-saxena/job-track && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "@theme" src/client/index.css
    - grep -q "@custom-variant dark" src/client/index.css
    - grep -q "backdrop-filter" src/client/index.css
    - grep -q "-webkit-backdrop-filter" src/client/index.css
    - grep -q "linear-gradient" src/client/index.css
    - grep -q "stroke-width: 1.8" src/client/index.css
    - grep -q "color-status-applied" src/client/index.css
    - grep -q "color-text-primary" src/client/index.css
    - grep -q "radius-card" src/client/index.css
    - grep -q "font-sans" src/client/index.css
    - grep -q "localStorage.theme" index.html
    - grep -q "prefers-color-scheme" index.html
    - grep -q "tanstackRouter" vite.config.ts
    - grep -q "routeTree.gen.ts" vite.config.ts
    - grep -q "useTheme" src/client/hooks/useTheme.ts
    - grep -q "localStorage" src/client/hooks/useTheme.ts
    - grep -q "STATUS_COLORS" src/client/lib/colors.ts
    - grep -q "STATUS_LABELS" src/client/lib/colors.ts
    - test -f node_modules/@tanstack/react-router/package.json
    - test -f node_modules/lucide-react/package.json
    - test -f node_modules/zustand/package.json
  </acceptance_criteria>
  <done>
    npm dependencies installed (tanstack router, query, lucide, zustand). Vite config has tanstackRouter plugin first. index.html has FOUC prevention script. index.css has complete @theme tokens, @custom-variant dark, glass utilities with Safari prefix, warm gradient body, Lucide 1.8px stroke. useTheme Zustand store works with light/dark/system modes and localStorage persistence. colors.ts exports STATUS_COLORS, STATUS_LABELS, STATUS_BADGE_BG maps.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build all 9 design system components</name>
  <files>
    src/client/components/design-system/Badge.tsx,
    src/client/components/design-system/Button.tsx,
    src/client/components/design-system/Input.tsx,
    src/client/components/design-system/Card.tsx,
    src/client/components/design-system/ColumnHeader.tsx,
    src/client/components/design-system/SearchBar.tsx,
    src/client/components/design-system/FilterChips.tsx,
    src/client/components/design-system/TabBar.tsx,
    src/client/components/design-system/Modal.tsx,
    src/client/components/design-system/index.ts
  </files>
  <read_first>
    src/client/index.css (just created — for token names),
    src/client/lib/colors.ts (just created — for status color mapping),
    src/client/components/ui/Button.tsx (existing pattern — forwardRef, interface props),
    src/client/components/ui/Input.tsx (existing pattern),
    .planning/phases/03-frontend-shell-dashboard/03-UI-SPEC.md (Components section),
    .planning/phases/03-frontend-shell-dashboard/design-system.md
  </read_first>
  <action>
Create directory `src/client/components/design-system/` with 9 component files + barrel index.ts. All components use Tailwind utilities generated from @theme tokens (e.g., `bg-status-applied`, `text-text-primary`, `rounded-[var(--radius-card)]`). All use `forwardRef` pattern from existing Button.tsx. No hardcoded hex colors — always use token-derived classes or CSS variables. Per D-23: NO emojis anywhere, Lucide icons only.

**Badge.tsx (per D-09, UI-SPEC Badge section):**
- Props: `variant: 'filled' | 'outlined' | 'dot'`, `color: string` (status color key or hex), `size: 'sm' | 'md'`, `children: ReactNode`
- `filled`: soft bg at .12 alpha (use STATUS_BADGE_BG from colors.ts for status colors, or compute from hex), pill shape `rounded-pill` (99px), text color matches status color
- `outlined`: border matching status color, no fill, pill shape
- `dot`: small colored circle (6px) + plain text, no background
- Dark mode: slightly adjusted alpha for readability
- Use `cn()` helper or template literals for class composition

**Button.tsx (per D-09, UI-SPEC Button section — REPLACES existing src/client/components/ui/Button.tsx):**
- Props: `variant: 'filled' | 'outline' | 'ghost'`, `size: 'sm' | 'md'`, `color?: 'default' | 'destructive'`, `loading?: boolean`, `children`, standard button HTML attrs via forwardRef
- `filled`: `bg-surface-accent text-white` (#292524 bg), `hover:bg-[#1c1917]`. Dark: `bg-dark-accent text-dark-dominant`
- `outline`: `border border-black/10 bg-transparent`, hover subtle fill. Dark: `border-white/15`
- `ghost`: `bg-black/[0.04] hover:bg-black/[0.08]`, no border. Dark: `bg-white/[0.06] hover:bg-white/[0.1]`
- Radius: 6px (rounded-[var(--radius-btn)]). Sizes: sm=30px height, md=34px height
- `destructive` color: red bg/border variants
- Keep loading spinner SVG from existing Button

**Input.tsx (per D-09, UI-SPEC Input section — REPLACES existing src/client/components/ui/Input.tsx):**
- Props: `variant: 'glass' | 'raised'`, `label?: string`, `error?: string`, `hint?: string`, standard input HTML attrs via forwardRef
- `glass`: `.glass` utility class + `rounded-[var(--radius-input)]` (10px). Subtle border. Focus: darker border + ring
- `raised`: elevated with `shadow-sm`, slightly more opaque bg
- Also support `<textarea>` via an `as?: 'input' | 'textarea'` prop
- Label above in `text-text-secondary font-semibold text-xs uppercase tracking-wide`
- Error in red below. Hint in muted below (for markdown hint text).
- Dark mode: dark glass surface, light text

**Card.tsx (per D-09, UI-SPEC Glass Effect):**
- Props: `className?: string`, `hover?: boolean`, `children: ReactNode`
- Applies `.glass` class by default. If `hover` is true, also applies `.glass-hover`
- Radius: `rounded-[var(--radius-card)]` (8px desktop). Optional `padding` prop for convenience.
- This is the building block for kanban cards, detail page cards, modal inner wrapper, etc.

**ColumnHeader.tsx (per D-09, D-14, UI-SPEC ColumnHeader section):**
- Props: `status: ApplicationStatus`, `count: number`, `variant: 'filled' | 'minimal'`
- `filled`: subtle bg matching status at .08 alpha, colored dot (8px circle matching STATUS_COLORS[status]), status label text, count badge (Badge component, filled variant, small)
- `minimal`: uppercase, small, muted text, no bg, count inline
- Import `STATUS_COLORS`, `STATUS_LABELS` from `@/client/lib/colors`

**SearchBar.tsx (per D-09, D-03, UI-SPEC SearchBar section):**
- Props: `variant: 'glass' | 'raised'`, `placeholder?: string`, `value?: string`, `onChange?: (value: string) => void`
- Glass variant: uses `.glass` utility, `rounded-[var(--radius-input)]`, search icon (lucide `Search`) left, Cmd+K badge right (small muted text in rounded rect)
- Raised variant: `shadow-sm`, more opaque bg
- Controlled input with Lucide Search icon (size 16, strokeWidth 1.8)

**FilterChips.tsx (per D-09, UI-SPEC FilterChips section):**
- Props: `items: { label: string; value: string; count?: number }[]`, `active: string`, `onChange: (value: string) => void`, `variant: 'tab' | 'outlined' | 'underline'`
- `tab`: square with subtle bg on active (bg-black/[0.06] active, transparent inactive)
- `outlined`: border, darker border + subtle fill on active
- `underline`: just bottom underline on active
- Render as a horizontal scrollable row (`flex gap-2 overflow-x-auto`)

**TabBar.tsx (per D-09, UI-SPEC TabBar section):**
- Props: `items: { label: string; value: string; count?: number }[]`, `active: string`, `onChange: (value: string) => void`, `variant: 'underline' | 'chip'`
- `underline`: active tab has `border-b-2 border-surface-accent text-text-primary font-semibold`, inactive is `text-text-muted`
- `chip`: active tab has subtle bg fill (bg-black/[0.06]), rounded
- Both: horizontal scroll on mobile with `scrollbar-width: none` and `-webkit-scrollbar { display: none }`

**Modal.tsx (per D-04, D-09, UI-SPEC Modal section):**
- Props: `open: boolean`, `onClose: () => void`, `title: string`, `children: ReactNode`
- Renders only when `open` is true (return null otherwise)
- Backdrop: `fixed inset-0 z-40 bg-black/30`, click to close
- Desktop: centered `.glass` card, `max-w-md`, `rounded-[var(--radius-modal)]` (14px)
- Mobile (< md): bottom sheet — `items-end p-0`, card has `rounded-b-none rounded-t-[14px]`, grab handle div (h-1 w-8 rounded-full bg-black/10)
- Close on Escape key (useEffect with keydown listener)
- Focus trap not required for Phase 3 (can add in Phase 9 polish)

**index.ts barrel export:**
Re-export all 9 components from `@/client/components/design-system`.
  </action>
  <verify>
    <automated>cd /Users/akhilsaxena/Documents/Repositories/akhil-saxena/job-track && npx tsc --noEmit -p tsconfig.app.json 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "export function Badge" src/client/components/design-system/Badge.tsx
    - grep -q "variant.*filled.*outlined.*dot" src/client/components/design-system/Badge.tsx
    - grep -q "export.*Button" src/client/components/design-system/Button.tsx
    - grep -q "variant.*filled.*outline.*ghost" src/client/components/design-system/Button.tsx
    - grep -q "export.*Input" src/client/components/design-system/Input.tsx
    - grep -q "variant.*glass.*raised" src/client/components/design-system/Input.tsx
    - grep -q "export function Card" src/client/components/design-system/Card.tsx
    - grep -q "glass" src/client/components/design-system/Card.tsx
    - grep -q "export function ColumnHeader" src/client/components/design-system/ColumnHeader.tsx
    - grep -q "STATUS_COLORS\|STATUS_LABELS" src/client/components/design-system/ColumnHeader.tsx
    - grep -q "export function SearchBar" src/client/components/design-system/SearchBar.tsx
    - grep -rq "lucide-react" src/client/components/design-system/SearchBar.tsx
    - grep -q "export function FilterChips" src/client/components/design-system/FilterChips.tsx
    - grep -q "export function TabBar" src/client/components/design-system/TabBar.tsx
    - grep -q "export function Modal" src/client/components/design-system/Modal.tsx
    - grep -q "Escape" src/client/components/design-system/Modal.tsx
    - test -f src/client/components/design-system/index.ts
  </acceptance_criteria>
  <done>
    All 9 design system components created with TypeScript interfaces, variant props, dark mode support via Tailwind dark: prefix, and token-derived styling. Badge has filled/outlined/dot. Button has filled/outline/ghost (replaces old Button). Input has glass/raised with label/error/hint. Card wraps glass effect. ColumnHeader has filled/minimal with status colors. SearchBar has glass/raised with Lucide icon and Cmd+K badge. FilterChips has tab/outlined/underline. TabBar has underline/chip. Modal has desktop centered/mobile bottom sheet with Escape close. Barrel index.ts re-exports all.
  </done>
</task>

</tasks>

<verification>
- `npm run build` succeeds with zero errors
- `npx tsc --noEmit -p tsconfig.app.json` passes
- `npm test` passes (existing API tests not broken by vite.config.ts changes)
- All @theme tokens present in index.css
- All 9 components exist with correct variant props
- Dark mode FOUC prevention script in index.html head
</verification>

<success_criteria>
1. Tailwind v4 @theme generates working utility classes for all design tokens (status colors, text colors, radii, fonts)
2. Glass effect renders correctly in both light and dark mode
3. Dark mode has FOUC prevention, system preference detection, manual toggle capability, and localStorage persistence
4. All 9 design system components are type-safe, use token-derived styles (no hardcoded hex), support dark mode, and match UI-SPEC
5. Existing API test suite still passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-frontend-shell-dashboard/03-01-SUMMARY.md`
</output>
