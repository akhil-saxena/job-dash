---
phase: 02-application-tracking-api
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - tests/setup.ts
  - tests/applications/crud.test.ts
  - tests/applications/status.test.ts
  - tests/applications/softdelete.test.ts
  - tests/applications/archive-pin.test.ts
autonomous: true
requirements: [TRACK-01, TRACK-02, TRACK-03, TRACK-04, TRACK-05, TRACK-06, TRACK-07, TRACK-08]

must_haves:
  truths:
    - "Integration tests prove CRUD operations work end-to-end against D1 via Cloudflare test worker"
    - "Tests verify status change generates timeline event atomically"
    - "Tests verify soft-delete excludes from default list and restore brings it back"
    - "Tests verify archive excludes from default list but appears with ?archived=true"
    - "Tests verify pin toggle persists and generates timeline event"
    - "Tests verify tenant isolation -- user A cannot read/modify user B's applications"
    - "Tests verify slug auto-generation and collision handling"
    - "All tests pass via npm test"
  artifacts:
    - path: "tests/setup.ts"
      provides: "Test database setup with application and timeline_event migration SQL"
      contains: "application"
    - path: "tests/applications/crud.test.ts"
      provides: "Tests for TRACK-01 (create), TRACK-02 (update), D-16 (tenant isolation)"
      min_lines: 80
    - path: "tests/applications/status.test.ts"
      provides: "Tests for TRACK-06 (status enum), TRACK-07 (status change), TRACK-08 (timeline events)"
      min_lines: 60
    - path: "tests/applications/softdelete.test.ts"
      provides: "Tests for TRACK-03 (soft delete + restore)"
      min_lines: 40
    - path: "tests/applications/archive-pin.test.ts"
      provides: "Tests for TRACK-04 (archive), TRACK-05 (pin)"
      min_lines: 50
  key_links:
    - from: "tests/applications/crud.test.ts"
      to: "worker/index.ts"
      via: "SELF_WORKER binding for integration testing"
      pattern: "SELF.*fetch"
    - from: "tests/setup.ts"
      to: "src/db/schema/application.ts"
      via: "Migration SQL must match schema exactly"
      pattern: "application"
---

<objective>
Write comprehensive integration tests covering all 8 TRACK requirements, tenant isolation, slug generation, and timeline event generation. Tests run against the Cloudflare Workers test pool with real D1 database.

Purpose: These tests are the verification gate for the entire phase. They prove the API works end-to-end -- from HTTP request through Hono route through service layer through Drizzle to D1 and back. Without passing tests, the phase cannot be verified.

Output: 5 files -- updated test setup with new table migrations, plus 4 test files covering all requirements.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-application-tracking-api/02-CONTEXT.md
@.planning/phases/02-application-tracking-api/02-RESEARCH.md
@.planning/phases/02-application-tracking-api/02-01-SUMMARY.md
@.planning/phases/02-application-tracking-api/02-02-SUMMARY.md

@tests/setup.ts
@src/db/schema/application.ts
@src/server/routes/applications.ts
@src/server/services/application.ts
@src/shared/constants.ts
@src/shared/validators/application.ts
@worker/index.ts
@vitest.config.ts

<interfaces>
<!-- Test infrastructure from Phase 1 -->

From tests/setup.ts (pattern to follow):
```typescript
import { env } from "cloudflare:test";
const migrationSQL = `...CREATE TABLE statements...`;
await env.DB.exec(migrationSQL);
```

From vitest.config.ts (test runner config):
```typescript
// Uses cloudflareTest() vitest plugin with Workers pool
// Tests run in Workers runtime with env.DB = D1 binding
```

Test pattern (from Phase 1 auth tests):
```typescript
import { env, SELF } from "cloudflare:test";
import { describe, it, expect, beforeEach } from "vitest";

// SELF is the worker binding -- use SELF.fetch() to make HTTP requests
// env.DB is the D1 database binding -- use for direct DB setup/assertions
```

API response format (per D-01):
```typescript
// Success: { data: T }
// List: { data: T[], pagination: { page, limit, total, totalPages } }
// Error: { data: null, error: { code: string, message: string } }
```

API endpoints (from Plan 02):
```
POST   /api/applications           -- create
GET    /api/applications           -- list (with query params)
GET    /api/applications/:id       -- get by id
PATCH  /api/applications/:id       -- update
PATCH  /api/applications/:id/status  -- change status
PATCH  /api/applications/:id/pin     -- toggle pin
PATCH  /api/applications/:id/archive -- toggle archive
DELETE /api/applications/:id       -- soft delete
PATCH  /api/applications/:id/restore -- restore
GET    /api/applications/:id/timeline -- get timeline
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update test setup and create CRUD + tenant isolation tests</name>
  <files>tests/setup.ts, tests/applications/crud.test.ts</files>
  <read_first>tests/setup.ts, src/db/schema/application.ts, src/db/migrations/0000_tricky_leo.sql, worker/index.ts, vitest.config.ts, .planning/phases/02-application-tracking-api/02-RESEARCH.md</read_first>
  <action>
**tests/setup.ts** -- Add application and timeline_event table CREATE statements to the existing migration SQL string.

CRITICAL: First run `npx drizzle-kit generate` to generate the new migration file for the application tables. Then read the generated SQL from `src/db/migrations/` to get the exact CREATE TABLE statements. Append these statements to the existing `migrationSQL` string in tests/setup.ts. Do NOT hand-write the SQL -- use the drizzle-kit generated SQL to ensure exact match with the schema.

The resulting setup.ts should have ALL tables (auth tables from Phase 1 + application + timeline_event) in the migrationSQL string, executed via `env.DB.exec(migrationSQL)`.

**tests/applications/crud.test.ts** -- Integration tests using SELF.fetch() against the worker.

IMPORTANT: Tests need an authenticated session. Create a helper function `createTestUser(db)` that directly inserts a user + session into the D1 database via `env.DB.exec()` or Drizzle, and returns `{ userId, sessionToken }`. Then pass the session token as a cookie header: `headers: { Cookie: "better-auth.session_token=" + sessionToken }`. Alternatively, check how Phase 1 auth tests handle authentication and follow that exact pattern.

If the auth test pattern uses `better-auth.session_token` cookie, follow that. If it uses a different mechanism, match it.

**Test cases for crud.test.ts:**

describe("POST /api/applications"):
- "creates application with required fields" -- POST with `{ companyName: "Google", roleTitle: "Senior SDE" }`. Assert 201 status. Assert response has `data.id`, `data.slug` equals "google-senior-sde", `data.status` equals "wishlist" (default), `data.isPinned` is false, `data.isArchived` is false. (TRACK-01)
- "creates application with all optional fields" -- POST with all fields from D-13: companyName, roleTitle, jobPostingUrl, locationType, locationCity, salaryMin, salaryMax, salaryCurrency, source, priority, notes, appliedAt. Assert all fields present in response. (TRACK-01)
- "generates unique slug on collision" -- Create two apps with same company+role. Assert second slug is "google-senior-sde-2". (D-14)
- "rejects missing required fields" -- POST with empty body. Assert 400 status. (D-03)
- "rejects invalid status value" -- POST with `{ companyName: "X", roleTitle: "Y", status: "invalid" }`. Assert 400. (TRACK-06)
- "auto-generates created timeline event" -- Create app, then GET /api/applications/:id/timeline. Assert timeline has one event with eventType "created". (D-09, TRACK-08)

describe("GET /api/applications"):
- "returns paginated list" -- Create 3 apps. GET /api/applications. Assert response has `data` array with 3 items and `pagination` with total=3. (D-01, D-02)
- "filters by status" -- Create apps with different statuses. GET ?status=applied. Assert only matching apps returned. (D-02)
- "filters by priority" -- Create apps with different priorities. GET ?priority=high. Assert only matching. (D-02)
- "searches by company name" -- GET ?search=goo. Assert apps with "Google" in company name returned. (D-02)
- "searches by role title" -- GET ?search=senior. Assert matching apps returned. (D-02)
- "paginates correctly" -- Create 5 apps. GET ?page=2&limit=2. Assert 2 items returned, pagination shows total=5, totalPages=3. (D-02)
- "excludes soft-deleted by default" -- Create app, soft-delete it, GET list. Assert deleted app not in list. (D-17)
- "excludes archived by default" -- Create app, archive it, GET list. Assert archived app not in list. (D-18)

describe("GET /api/applications/:id"):
- "returns application with timeline" -- Create app, GET by id. Assert data has all fields and timeline array. (TRACK-02)
- "returns 404 for non-existent id" -- GET with random id. Assert 404. (D-03)

describe("PATCH /api/applications/:id"):
- "updates single field" -- Create app, PATCH with `{ companyName: "Meta" }`. Assert response has updated companyName, other fields unchanged. (TRACK-02)
- "updates multiple fields" -- PATCH with companyName + priority. Assert both updated. (TRACK-02)
- "returns 404 for non-existent id" -- PATCH random id. Assert 404.

describe("tenant isolation"):
- "user A cannot read user B's application" -- Insert app directly for user B. User A GET /api/applications/:id. Assert 404. (D-16)
- "user A cannot update user B's application" -- Insert app directly for user B. User A PATCH. Assert 404. (D-16)
- "user A list does not include user B's applications" -- Insert app for user B. User A GET /api/applications. Assert empty list. (D-16)
  </action>
  <verify>
    <automated>cd /Users/akhilsaxena/Documents/Repositories/akhil-saxena/job-track && npx vitest run tests/applications/crud.test.ts --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "application" tests/setup.ts && grep -q "timeline_event" tests/setup.ts (migration SQL includes new tables)
    - grep -q "POST.*api/applications" tests/applications/crud.test.ts (create tests)
    - grep -q "GET.*api/applications" tests/applications/crud.test.ts (list tests)
    - grep -q "PATCH.*api/applications" tests/applications/crud.test.ts (update tests)
    - grep -q "isolation" tests/applications/crud.test.ts (tenant isolation tests per D-16)
    - grep -q "slug" tests/applications/crud.test.ts (slug generation tests per D-14)
    - All tests in crud.test.ts pass
  </acceptance_criteria>
  <done>Test setup includes application + timeline_event migration SQL. CRUD tests cover create with defaults, create with all fields, slug collision, validation rejection, list with filtering/search/pagination, get by id, update single/multiple fields, and tenant isolation (3 scenarios). All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Create status, soft-delete, archive, and pin tests</name>
  <files>tests/applications/status.test.ts, tests/applications/softdelete.test.ts, tests/applications/archive-pin.test.ts</files>
  <read_first>tests/applications/crud.test.ts, src/server/services/application.ts, src/server/routes/applications.ts, src/shared/constants.ts</read_first>
  <action>
Reuse the same test auth helper pattern from crud.test.ts (createTestUser, session cookie header).

**tests/applications/status.test.ts:**

describe("status enum validation"):
- "accepts all valid status values" -- For each of the 8 statuses in APPLICATION_STATUSES, create an app with that status. Assert 201 for all. (TRACK-06)
- "rejects invalid status value" -- POST with status "pending". Assert 400 validation error. (TRACK-06)

describe("PATCH /api/applications/:id/status"):
- "changes status and returns updated app" -- Create app (default wishlist). PATCH /status with `{ status: "applied" }`. Assert response data.status is "applied". (TRACK-07)
- "any-to-any transition allowed" -- Create app with "offer" status. Change to "wishlist". Assert success (no pipeline enforcement per D-06). (TRACK-06, D-06)
- "generates status_change timeline event" -- Create app. Change status. GET /timeline. Assert event with eventType "status_change", metadata contains `{ from: "wishlist", to: "applied" }`. (TRACK-08, D-08)
- "no-op when status unchanged" -- Create app with "applied". PATCH /status with "applied" again. Assert success but no new timeline event (only the "created" event should exist). (TRACK-08)
- "returns 404 for soft-deleted app" -- Create and soft-delete app. PATCH /status. Assert 404.

describe("timeline events"):
- "create generates created event" -- Create app. GET timeline. Assert one event with eventType "created". (D-09)
- "status change generates event with from/to metadata" -- Change status. Assert event metadata has correct from/to values per D-12.
- "multiple status changes generate multiple events" -- Change status twice. Assert 3 timeline events (1 created + 2 status_change), ordered by occurredAt desc. (TRACK-08)

**tests/applications/softdelete.test.ts:**

describe("DELETE /api/applications/:id"):
- "soft-deletes application (sets deleted_at)" -- Create app. DELETE. Assert response has deletedAt not null. (TRACK-03, D-17)
- "soft-deleted app excluded from list" -- Create and delete. GET list. Assert empty. (D-17)
- "soft-deleted app returns 404 on get by id" -- Create and delete. GET by id. Assert 404.
- "generates deleted timeline event" -- Create and delete. Check timeline has "deleted" event. (D-09)

describe("PATCH /api/applications/:id/restore"):
- "restores soft-deleted application" -- Create, delete, restore. Assert deletedAt is null in response. (TRACK-03)
- "restored app appears in list again" -- Create, delete, restore. GET list. Assert app present.
- "generates restored timeline event" -- Create, delete, restore. Check timeline has "restored" event. (D-09)
- "returns 404 when restoring non-deleted app" -- Create (not deleted). PATCH /restore. Assert 404 (only deleted apps can be restored).

**tests/applications/archive-pin.test.ts:**

describe("PATCH /api/applications/:id/archive"):
- "archives application" -- Create app. PATCH /archive. Assert isArchived is true. (TRACK-04)
- "archived app excluded from default list" -- Create and archive. GET /api/applications. Assert empty. (D-18)
- "archived app appears with ?archived=true" -- Create and archive. GET /api/applications?archived=true. Assert app present. (D-18)
- "unarchives on second toggle" -- Create, archive, archive again. Assert isArchived is false.
- "generates archived/unarchived timeline events" -- Archive then unarchive. Assert timeline has "archived" and "unarchived" events. (D-09)
- "archive and soft-delete are independent" -- Archive app, then soft-delete it. Both flags set independently per D-19.

describe("PATCH /api/applications/:id/pin"):
- "pins application" -- Create app. PATCH /pin. Assert isPinned is true. (TRACK-05)
- "unpins on second toggle" -- Create, pin, pin again. Assert isPinned is false. (TRACK-05)
- "pin persists across requests" -- Pin app. GET by id. Assert isPinned still true. (TRACK-05)
- "generates pinned/unpinned timeline events" -- Pin then unpin. Assert timeline has "pinned" and "unpinned" events. (D-09)
  </action>
  <verify>
    <automated>cd /Users/akhilsaxena/Documents/Repositories/akhil-saxena/job-track && npm test 2>&1 | tail -40</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "status_change" tests/applications/status.test.ts (timeline event verification per TRACK-08)
    - grep -q "any-to-any" tests/applications/status.test.ts (D-06 transition freedom)
    - grep -q "soft-delete" tests/applications/softdelete.test.ts (TRACK-03)
    - grep -q "restore" tests/applications/softdelete.test.ts (TRACK-03)
    - grep -q "archive" tests/applications/archive-pin.test.ts (TRACK-04)
    - grep -q "pin" tests/applications/archive-pin.test.ts (TRACK-05)
    - grep -q "timeline" tests/applications/status.test.ts (TRACK-08)
    - npm test passes with all test files green
  </acceptance_criteria>
  <done>Status tests verify all 8 enum values accepted, any-to-any transitions, status_change timeline events with from/to metadata, and no-op on same status. Soft-delete tests verify deleted_at set, excluded from list, 404 on get, restore clears deleted_at. Archive tests verify isArchived toggle, excluded from default list, appears with ?archived=true, independent of soft-delete. Pin tests verify isPinned toggle persists, timeline events generated. Full suite passes via npm test.</done>
</task>

</tasks>

<verification>
- `npm test` passes with all 4 test files green
- Every TRACK requirement has at least one passing test
- Tenant isolation explicitly tested (user A cannot access user B)
- Timeline events verified for all 8 event types from D-11
- Pagination verified with correct totalPages calculation
- Soft-delete leak prevention verified (deleted apps never in default list)
</verification>

<success_criteria>
- All tests pass via `npm test`
- TRACK-01: Create with all fields + slug generation tested
- TRACK-02: Update single/multiple fields tested
- TRACK-03: Soft-delete + restore tested
- TRACK-04: Archive/unarchive + exclusion from default list tested
- TRACK-05: Pin/unpin + persistence tested
- TRACK-06: All 8 status values accepted, invalid rejected
- TRACK-07: Status change via PATCH /status endpoint tested
- TRACK-08: Timeline events generated for all mutations, with correct metadata
- D-16: Tenant isolation verified with cross-user access denied
</success_criteria>

<output>
After completion, create `.planning/phases/02-application-tracking-api/02-03-SUMMARY.md`
</output>
